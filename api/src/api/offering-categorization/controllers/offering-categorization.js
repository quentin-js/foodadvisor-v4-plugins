'use strict';

/**
 *  offering-categorization controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::offering-categorization.offering-categorization');
