'use strict';

/**
 * offering-categorization service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::offering-categorization.offering-categorization');
